#!/bin/sh
exec 2>&1
echo "Starting..."

sleep 1

cd <%= @options[:dir] %>

USER=diaspora

exec chpst -u $USER \
/usr/bin/env HOME=<%= @options[:dir] %> \
             RAILS_ENV=<%= @options[:platform] %> \
bundle exec resque-web -F \
2>&1


